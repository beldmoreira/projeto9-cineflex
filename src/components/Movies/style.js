import styled from "styled-components";

const MovieStyle = styled.div`
display: flex;
background-color: #FFF;
padding: 8px;
border-radius: 3px;
box-shadow: 0px 2px 4px 2px rgba(0, 0, 0, 0.1);
margin-bottom: 11px;
`;

const MoviePosters = styled.img`
width: 129px;
height: 193px;
`;

export{
    MovieStyle,
    MoviePosters
}